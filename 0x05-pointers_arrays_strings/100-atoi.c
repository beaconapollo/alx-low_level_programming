#include "main.h"
#include <stdio.h>

/**
 * _atoi - converts a string to integer
 * @s: pointer of a string
 *
 * Return: int
 */
int _atoi(char *s)
{
	int i = 0, n = 0;

	while (*(s + i))
	{
		
		i++;
	}
	*(dest + i) = '\0';
	return (dest);
}
