#include "main.h"
/**
 * array_iterator - executes a function given a parameter on each element of an array
 * @size: the size of an array
 * @action: a pointer to the functio
 *
 * Return: void
 */
void array_iterator(int *array, size_t size, void (*action)(int))
{
	f(name);
}
