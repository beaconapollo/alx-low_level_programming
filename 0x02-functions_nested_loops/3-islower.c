#include "main.h"
/**
 * print_alphabet_x10 - prints the english alphabet a-z 10 times
 *
 * Return: Nothing.
 *
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}	
	return (0);
}
