#include "main.h"
#include <stdio.h>
/**
 * main - prints its name
 * @arc: number of command line arguments
 * @argv: commands
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv);
	return(0);
}
