#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - prints a string followed by a new line
 * @s: string to print
 *
 * Return: void
 */
int main(int argc, char *argv)
{
	printf("%s", argv);
	return(0);
}
